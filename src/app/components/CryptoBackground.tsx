export function CryptoBackground() {
  // Since the background is applied via global CSS on the body,
  // this component doesn't need to render anything.
  // It's kept for structural consistency with the provided example.
  return null;
}
